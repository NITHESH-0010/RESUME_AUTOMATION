# ---- 1. Gemini (resume content generation) ----
# Get from https://aistudio.google.com -> "Get API key"
GEMINI_MODEL=gemini-3.6-flash
GEMINI_API_KEY=

# ---- 2. Google Sheets (submission logging) ----
# Path INSIDE the container to a service-account JSON key
# (create in Google Cloud Console -> IAM & Admin -> Service Accounts,
#  enable the "Google Sheets API", then share your sheet with the
#  service account's email address as an Editor)
GOOGLE_SERVICE_ACCOUNT_FILE=service-account.json
GOOGLE_SHEET_ID=my_google_sheet_id_here
GOOGLE_SHEET_NAME=Sheet1

# ---- 3. Gmail (send resume PDF to candidate) ----
# Use a Gmail App Password, NOT your normal password:
# https://myaccount.google.com/apppasswords (requires 2FA enabled)
GMAIL_ADDRESS=MYEMAIL-@gmail.com
GMAIL_APP_PASSWORD=

# ---- 4. Telegram (admin notification) ----
# Message @BotFather on Telegram -> /newbot -> copy the token.
# Get your chat ID by messaging @userinfobot or @RawDataBot.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ---- Gotenberg (self-hosted PDF conversion, no key needed) ----
# Leave as-is when running via docker-compose (service name "gotenberg").
GOTENBERG_URL=http://localhost:3000